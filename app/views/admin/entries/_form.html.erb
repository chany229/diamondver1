<%= form_with(model: [:admin, entry]) do |form| %>
  <% if entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entry.errors.count, "error") %> prohibited this admin_entry from being saved:</h2>

      <ul>
        <% entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :user_id, value: current_user.id %>
  <div class="field">
    <%= form.text_field :title, placeholder: '标题' %>
  </div>

  <div class="field">
    <%= form.select :category_id, Category.all.map{|c| [c.title, c.id]} %>
  </div>

  <div class="field">
    <%= form.text_field :tag_list, placeholder: '多个标签用空格分开' %>
  </div>

<!--   <div class="field">
    <%#= form.text_field :desc, placeholder: '描述' %>
  </div> -->

  <div class="field">
    <%= form.rich_text_area :content %>
  </div>


<!--   <div class="field">
    <%#= form.label :deleted_at %>
    <%#= form.datetime_select :deleted_at %>
  </div> -->

  <div class="actions">
    <%= form.submit '提交' %>
  </div>
<% end %>
